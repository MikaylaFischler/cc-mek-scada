name: Lua Checks

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - latest
      - devel
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3.5.1
      - name: Luacheck
        uses: lunarmodules/luacheck@v1.1.0
        with:
          args: . --no-max-line-length -a -i 542 --globals _HOST term fs peripheral rs bit parallel colors textutils shell settings window read periphemu http os.sleep
